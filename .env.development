# Development only. An empty base makes every chord URL a relative path, which
# the Vite dev proxy (see vite.config.ts) forwards to the real Space — so
# `npm run dev` works without localhost being in anyone's CORS allowlist.
#
# Point the proxy elsewhere with DEV_CHORD_BACKEND in a .env.local.
VITE_CHORD_API_BASE=
